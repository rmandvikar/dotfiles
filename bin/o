#!/bin/sh

if [ $# -gt 0 ]; then
	$EDITOR "$@" &
else
	args=""
	while read data; do
		args=$args$data" "
	done
	case "$OSTYPE" in
		linux* | msys*)
			echo $args | xargs -r $EDITOR &
			;;
		darwin*)
			echo $args | xargs $EDITOR &
			;;
	esac
fi
