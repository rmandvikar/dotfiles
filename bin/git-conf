#!/bin/sh

#usage: git conf <alias>
#
# To pretty print git config (uses 'git config --get-regex' internally).

arg="$1"
if [ $# -eq 2 ]; then
	arg+=".$2"
fi
git config --get-regex "$arg" | sed '/^alias\./ d'
