#!/bin/sh

#usage:
#   ... | wrap \"
#   ... | wrap \'
#   ... | wrap \(
#   ... | wrap \( \)
#
# Wrap text with args.

start=${1}
end=${2-"$start"}
while read data; do
	# exec echo does not work
	echo "$start""$data""$end"
done
