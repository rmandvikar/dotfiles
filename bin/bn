#!/bin/sh

#usage:
#   bn <path>
#   ... | bn
#
# basename

# see http://sed.sourceforge.net/local/docs/emulating_unix.txt

command='
		s,.*/,,
		s,"$,,
		s,^",,
		'
if [ $# -gt 0 ]; then
	sed "$command" <<< "$1"
else
	sed "$command"
fi
