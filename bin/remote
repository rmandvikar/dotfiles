#!/bin/sh

#usage: remote
#
# Browse to remote's url.

git config --get-regex remote\..*\.url \
	| cut -d' ' -f2 \
	| sed 's/\.git$//' | sed 's/^git@//' \
	| sed 's,^https\?://,,' | sed 's,:,/,' \
	| xargs start chrome --new-window
