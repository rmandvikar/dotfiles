#!/bin/sh
#
#usage:
#   ... | pipe <command>
#
# Pipe a command that does not support piping (ex echo, cp, etc).

while IFS= read -r line; do
	eval "$@" "$line"
done
if [ -n "$line" ]; then
	eval "$@" "$line"
fi
