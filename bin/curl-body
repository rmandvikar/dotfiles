#!/bin/sh

#usage:
#   ... | curl-body
#
# Print curl body.

sed '/^HTTP/ {n;d;}' | `# remove line after HTTP` \
	sed -n '/^$/,$ p' | `# print empty line till end` \
	sed '/^$/ d' `# remove empty lines`
